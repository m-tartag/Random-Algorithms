function confirmEnding(str, target) {
  const REV_STR = str
    .split('')
    .reverse()
    .join('');
  const REV_TAR = target
    .split('')
    .reverse()
    .join('');
}

confirmEnding('Bastian', 'n');
